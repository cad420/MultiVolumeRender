#pragma once

namespace shader{
    const char* raycast_pos_v=R"(@raycast_pos_v.glsl@)";

    const char* raycast_pos_f=R"(@raycast_pos_f.glsl@)";

    const char* raycast_render_v=R"(@raycast_render_v.glsl@)";

    const char* raycast_render_f=R"(@raycast_render_f.glsl@)";
}